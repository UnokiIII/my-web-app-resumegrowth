# 一人企业项目交接说明（2026-03-25）

## 1. 当前主项目
- 工作仓库：`D:\一人企业\archive\temp\my-web-app-resumegrowth`
- GitHub 仓库：`https://github.com/UnokiIII/my-web-app-resumegrowth`
- 当前主分支：`main`
- 本次整理时最新提交：`c6be786 fix: add baidu ocr fallback`

## 2. 这份备份包包含什么
- `app/`：Next.js 页面、API 路由、后台页
- `components/`：前端结果页、后台管理组件
- `lib/`：分析引擎、支付墙、Supabase、OCR 辅助逻辑
- `public/`：分享封面、支付二维码、加好友二维码
- `source-assets/`：封面图、收款码、加好友二维码原始素材
- `scripts/`：PDF 辅助脚本
- `.env.example`：环境变量模板
- `.env.local`：本机实际环境变量（仅本地使用，含敏感信息）
- `Microsoft.PowerShell_profile.ps1`：PowerShell UTF-8 编码配置
- `git-log.txt`：最近提交记录
- `git-status.txt`：整理时 git 状态
- `_deleted_temp_files/`：从原工作目录移出的临时文件/构建产物

## 3. 当前线上/本地功能结论
- 网站主功能：上传简历，生成一人企业成长方案
- 付费逻辑：微信收款码 + 加好友二维码 + 后台生成一次性解锁码
- 后台地址：`/admin/paywall`
- 后台密码：通过环境变量 `PAYWALL_ADMIN_PASSWORD` 控制
- 存储：订单/解锁码走 Supabase
- 分享图：`public/og-cover.png`

## 4. OCR 当前链路
### 4.1 PDF 主路径
1. 优先尝试提取 PDF 文本层
2. 如果文本层不足，则进入 OCR 兜底

### 4.2 OCR 兜底顺序
1. 外部兼容 OCR 网关
2. 百度 OCR
3. DashScope 原生 OCR

### 4.3 关键代码
- `app/api/analyze/route.ts`
- 关键函数：
  - `ocrImageWithExternalCompatibleV2`
  - `ocrImageWithBaiduV2`
  - `ocrImageWithDashScopeNativeV2`
  - `ocrPdfWithFallbackChain`

## 5. 支付与解锁逻辑
### 5.1 用户侧
1. 免费查看摘要
2. 微信扫码支付
3. 添加微信并发送订单号
4. 人工返回一次性解锁码
5. 输入后解锁当前报告

### 5.2 后台侧
- 后台页：`app/admin/paywall/page.tsx`
- 可做的事：
  - 按订单号搜索
  - 生成一次性解锁码
  - 复制解锁码
  - 标记已手动发送
  - 查看是否已兑换

## 6. Supabase 相关
### 6.1 已使用的环境变量
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `SUPABASE_BUCKET=payment-proofs`

### 6.2 表
- 表名：`payment_proofs`

### 6.3 主要代码
- `lib/supabase-server.ts`
- `lib/paywall-proof-store.ts`
- `app/api/paywall/create-order/route.ts`
- `app/api/paywall/redeem-code/route.ts`
- `app/api/admin/paywall/generate-code/route.ts`
- `app/api/admin/paywall/mark-sent/route.ts`

## 7. Vercel 上线时要有的环境变量
- `DASHSCOPE_API_KEY`
- `OCR_EXTERNAL_BASE_URL`
- `OCR_EXTERNAL_API_KEY`
- `OCR_EXTERNAL_MODEL_IDS`
- `BAIDU_OCR_API_KEY`
- `BAIDU_OCR_SECRET_KEY`
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `SUPABASE_BUCKET`
- `PAYWALL_ADMIN_PASSWORD`
- `NEXT_PUBLIC_PAYWALL_PRICE_LABEL`
- `NEXT_PUBLIC_PAYWALL_PAYMENT_ENTRY_MODE`

## 8. PowerShell 中文乱码修复
- 文件：`Microsoft.PowerShell_profile.ps1`
- 作用：把 PowerShell 输入/输出编码固定为 UTF-8
- 如果新开的 PowerShell 才生效，属正常
- 当前 profile 内容：
  - 设置 `InputEncoding`
  - 设置 `OutputEncoding`
  - 执行 `chcp 65001`

## 9. 其他 agent 接手时的建议顺序
1. 先看 `git-log.txt`
2. 再看 `app/api/analyze/route.ts`
3. 再看 `app/admin/paywall/page.tsx`
4. 再看 `lib/paywall-proof-store.ts` 与 `lib/supabase-server.ts`
5. 最后看 `.env.example`

## 10. 风险提示
- `.env.local` 内有真实敏感信息，只适合本机备份
- 百度/Supabase 等密钥如果曾在聊天中暴露，建议后续旋转
- 扫描版 PDF 仍然依赖外部 OCR，稳定性取决于上游接口
